import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com'],
    // i love unsplash avarude images ellam high quality aan pinne copyright free aan.
  },
};

export default nextConfig;
